# 参与贡献

欢迎通过 Issue 提供可复现的问题，或通过 PR 改进 Skill、说明与自制示例。涉及教学路线、输入格式或输出范围的变化，请先说明具体使用情境和验证边界。

## 提交方式

从最新 `main` 创建分支，通过 PR 合并。仓库保护要求 `Repository checks` 通过、分支包含最新 `main`，并解决审阅对话；管理员也受同样约束。禁止强推或删除 `main`，使用 squash merge，合并后自动删除功能分支。

当前采用个人维护配置：要求 PR，但不强制另一位审批人签字。后续有固定共同维护者时再提高审批人数；不要为合并日常改动临时关闭保护规则。

## 改动要求

- 遵循 [AGENTS.md](AGENTS.md)，代码缩进使用 Tab；YAML 等禁止 Tab 缩进的格式使用其合法语法。
- 保留原课件的顺序、数学符号、公式标注及条件，明确区分来源与新增解释。
- 只提交自制或明确允许再分发的样本，不包含私人课件、真实学生记录、凭据或个人绝对路径。
- 更新已有验证说明，区分实际执行、人工模拟和未验证内容，不把格式检查或样例成功说成真实学习效果。
- 项目自制内容按 [MIT](LICENSE) 提供；第三方材料须说明来源与适用许可。

## 检查

基础检查无 npm 安装步骤，使用 Node.js 22 或更高版本：

```sh
node validation/check.cjs
PYTHONPYCACHEPREFIX=tmp/pycache python3 -m py_compile examples/binary-search/build_example.py
git diff --check
```

它检查本项目使用的简单行内 Markdown 本地链接、许可证与共享样式副本，以及算法结果、不变量和无效输入；不替代完整 Markdown/YAML 解析、官方 Skill 格式校验或人工教学审阅。

修改 HTML、CSS、交互或原图时，按 [README](README.md#重复验证) 执行浏览器检查，并人工查看相关截图与打印页面。仅更改教学规则时提供对应行为样例及检查结果。PPTX、扫描件或其他新支持范围需要相应实际产物与验证，不能只更新支持声明。

## 报告问题

请给出复现步骤、预期与实际结果、相关文件或页码，以及脱敏后的最小样例。不要在公开 Issue 上传私人课件、学生回答或秘密信息。若问题涉及敏感信息，请先发不含敏感内容的说明，再与维护者确认合适的传递方式。
